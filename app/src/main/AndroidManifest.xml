<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user1.assignmentjobseekerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            android:label = "@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.package.name.MyActivity" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".job_providerlogin" />
        <activity android:name=".seeker_signup" />
        <activity android:name=".job_seekerlogin" />
        <activity android:name=".provider_signup" />
        <activity android:name=".forgotpwd" />
        <activity android:name=".post_job" />
        <activity android:name=".joblist"             android:noHistory="true"
        />
        <activity android:name=".job_apply" />
        <activity android:name=".go_profile"></activity>
    </application>

</manifest>